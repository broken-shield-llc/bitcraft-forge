# FORGE — copy to .env (FORGE_* keys only)

FORGE_DISCORD_TOKEN=
FORGE_DISCORD_APPLICATION_ID=
FORGE_DISCORD_GUILD_ID=

FORGE_BITCRAFT_WS_URI=wss://bitcraft-early-access.spacetimedb.com
FORGE_BITCRAFT_MODULE=bitcraft-1
# Required for SpacetimeDB auth (BitCraft session JWT). Empty = no STDB connection.
FORGE_BITCRAFT_JWT=

FORGE_DATABASE_URL=postgresql://forge:forge@localhost:5432/forge

FORGE_POC_EVENT_LOG=./data/poc-events.jsonl
FORGE_LOG_LEVEL=info

FORGE_ANNOUNCEMENT_DEBOUNCE_MS=4000
# Set to 0 to announce only new trade orders (no trade_order_state updates). Completions still announce.
# FORGE_QUEST_ANNOUNCE_TRADE_UPDATES=1
# After a barter completion, suppress "Quest Updated" for that order for this long (default: debounce + 3000 ms).
# FORGE_QUEST_COMPLETION_SUPPRESS_UPDATE_MS=7000
# Skip Discord quest announces briefly after STDB snapshot (replay / reconnect).
FORGE_QUEST_ANNOUNCE_GRACE_MS=4000

FORGE_STDB_CACHE_TTL_MS=86400000

# Optional HTTPS image URL shown as a banner on /forge quest board (e.g. PNG/WebP hosted on a CDN).
# FORGE_QUEST_BOARD_BANNER_URL=
